{
  "name": "portifolio-bernardo-iannini",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio pessoal de Bernardo Iannini - HTML/CSS/JS vanilla com gerador estatico de posts (Newsroom).",
  "scripts": {
    "build:posts": "node scripts/build-posts.js",
    "minify": "node scripts/minify-assets.js",
    "build": "npm run build:posts && npm run minify",
    "dev": "npx serve . -p 8080",
    "admin": "node scripts/admin-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "optionalDependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
